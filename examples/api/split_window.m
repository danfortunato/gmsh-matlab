% split_window.m
%
% Port of split_window.py. The Python version exits immediately under
% -nopopup; on macOS inside MATLAB the FLTK calls conflict with the Cocoa
% main thread anyway, so we exit the same way.
return
